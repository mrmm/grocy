package hooks

import (
	"github.com/pocketbase/pocketbase"
	"github.com/pocketbase/pocketbase/core"
)

// RegisterBatteries registers future batteries custom routes or hooks.
// Currently acts as placeholder so that the collection can be created manually via admin UI.
func RegisterBatteries(app *pocketbase.PocketBase, e *core.ServeEvent) {
	// placeholder – no custom endpoints yet
}
